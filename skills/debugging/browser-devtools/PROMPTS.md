# Browser DevTools Prompts

## Build
Use the Browser DevTools skill. Inspect existing project conventions, implement the requested change using idiomatic Browser DevTools patterns, verify with console error review, network trace inspection, performance profiles, DOM checks, and device emulation, and summarize changed files and risks.

## Review
Use the Browser DevTools skill. Review the change for correctness, maintainability, security, performance, and Browser DevTools-specific pitfalls such as debugging minified output only, ignoring network failures, changing CSS blindly, and not reproducing on target viewport. Provide findings by severity.

## Debug
Use the Browser DevTools skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with console error review, network trace inspection, performance profiles, DOM checks, and device emulation.

## Document
Use the Browser DevTools skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's Browser DevTools implementation.


# Java Prompts

## Build
Use the Java skill. Inspect existing project conventions, implement the requested change using idiomatic Java patterns, verify with unit tests, integration tests, static analysis, build lifecycle checks, and JVM runtime smoke tests, and summarize changed files and risks.

## Review
Use the Java skill. Review the change for correctness, maintainability, security, performance, and Java-specific pitfalls such as over-abstracted class hierarchies, unchecked nulls, blocking calls in async paths, and untested serialization. Provide findings by severity.

## Debug
Use the Java skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with unit tests, integration tests, static analysis, build lifecycle checks, and JVM runtime smoke tests.

## Document
Use the Java skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's Java implementation.


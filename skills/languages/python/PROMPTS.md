# Python Prompts

## Build
Use the Python skill. Inspect existing project conventions, implement the requested change using idiomatic Python patterns, verify with pytest, type checks, linting, import checks, and focused command-line smoke tests, and summarize changed files and risks.

## Review
Use the Python skill. Review the change for correctness, maintainability, security, performance, and Python-specific pitfalls such as global environments, unpinned dependencies, hidden mutable defaults, broad exception handlers. Provide findings by severity.

## Debug
Use the Python skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with pytest, type checks, linting, import checks, and focused command-line smoke tests.

## Document
Use the Python skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's Python implementation.


# PyTorch Prompts

## Build
Use the PyTorch skill. Inspect existing project conventions, implement the requested change using idiomatic PyTorch patterns, verify with shape tests, small-batch overfit checks, checkpoint load tests, deterministic seeds, and inference smoke tests, and summarize changed files and risks.

## Review
Use the PyTorch skill. Review the change for correctness, maintainability, security, performance, and PyTorch-specific pitfalls such as silent shape bugs, device mismatch, missing eval mode, unstable training loops, and untracked experiment config. Provide findings by severity.

## Debug
Use the PyTorch skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with shape tests, small-batch overfit checks, checkpoint load tests, deterministic seeds, and inference smoke tests.

## Document
Use the PyTorch skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's PyTorch implementation.


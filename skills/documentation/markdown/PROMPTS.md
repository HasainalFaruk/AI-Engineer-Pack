# Markdown Prompts

## Build
Use the Markdown skill. Inspect existing project conventions, implement the requested change using idiomatic Markdown patterns, verify with heading review, link checks, code fence balance, command accuracy, and readability pass, and summarize changed files and risks.

## Review
Use the Markdown skill. Review the change for correctness, maintainability, security, performance, and Markdown-specific pitfalls such as broken relative links, stale commands, inconsistent headings, placeholder text, and examples without context. Provide findings by severity.

## Debug
Use the Markdown skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with heading review, link checks, code fence balance, command accuracy, and readability pass.

## Document
Use the Markdown skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's Markdown implementation.


# Drupal Prompts

## Build
Use the Drupal skill. Inspect existing project conventions, implement the requested change using idiomatic Drupal patterns, verify with cache rebuilds, config import/export checks, permission tests, and module install smoke tests, and summarize changed files and risks.

## Review
Use the Drupal skill. Review the change for correctness, maintainability, security, performance, and Drupal-specific pitfalls such as hard-coded config, missing access callbacks, cache metadata mistakes, and update hooks without rollback thinking. Provide findings by severity.

## Debug
Use the Drupal skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with cache rebuilds, config import/export checks, permission tests, and module install smoke tests.

## Document
Use the Drupal skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's Drupal implementation.


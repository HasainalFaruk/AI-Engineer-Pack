# PostgreSQL Prompts

## Build
Use the PostgreSQL skill. Inspect existing project conventions, implement the requested change using idiomatic PostgreSQL patterns, verify with migration dry runs, EXPLAIN analysis, constraint tests, rollback review, and query regression checks, and summarize changed files and risks.

## Review
Use the PostgreSQL skill. Review the change for correctness, maintainability, security, performance, and PostgreSQL-specific pitfalls such as missing indexes, unsafe locks, unbounded queries, weak constraints, and irreversible migrations. Provide findings by severity.

## Debug
Use the PostgreSQL skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with migration dry runs, EXPLAIN analysis, constraint tests, rollback review, and query regression checks.

## Document
Use the PostgreSQL skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's PostgreSQL implementation.


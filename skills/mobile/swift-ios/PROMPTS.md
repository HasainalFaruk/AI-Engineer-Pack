# Swift iOS Prompts

## Build
Use the Swift iOS skill. Inspect existing project conventions, implement the requested change using idiomatic Swift iOS patterns, verify with unit tests, UI smoke tests, concurrency checks, simulator runs, and permission flow validation, and summarize changed files and risks.

## Review
Use the Swift iOS skill. Review the change for correctness, maintainability, security, performance, and Swift iOS-specific pitfalls such as main-thread blocking, force unwraps, missing privacy strings, retain cycles, and unhandled offline states. Provide findings by severity.

## Debug
Use the Swift iOS skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with unit tests, UI smoke tests, concurrency checks, simulator runs, and permission flow validation.

## Document
Use the Swift iOS skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's Swift iOS implementation.


# React Native Prompts

## Build
Use the React Native skill. Inspect existing project conventions, implement the requested change using idiomatic React Native patterns, verify with iOS and Android smoke tests, component tests, navigation checks, native build checks, and device permission tests, and summarize changed files and risks.

## Review
Use the React Native skill. Review the change for correctness, maintainability, security, performance, and React Native-specific pitfalls such as assuming platform parity, slow lists, unhandled permissions, native dependency drift, and layout issues on small screens. Provide findings by severity.

## Debug
Use the React Native skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with iOS and Android smoke tests, component tests, navigation checks, native build checks, and device permission tests.

## Document
Use the React Native skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's React Native implementation.


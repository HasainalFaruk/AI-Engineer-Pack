# OpenAI API Prompts

## Build
Use the OpenAI API skill. Inspect existing project conventions, implement the requested change using idiomatic OpenAI API patterns, verify with schema validation, eval sets, prompt regression tests, token and latency checks, and failure-mode tests, and summarize changed files and risks.

## Review
Use the OpenAI API skill. Review the change for correctness, maintainability, security, performance, and OpenAI API-specific pitfalls such as unbounded prompts, missing evals, fragile JSON parsing, tool-call ambiguity, and no fallback behavior. Provide findings by severity.

## Debug
Use the OpenAI API skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with schema validation, eval sets, prompt regression tests, token and latency checks, and failure-mode tests.

## Document
Use the OpenAI API skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's OpenAI API implementation.


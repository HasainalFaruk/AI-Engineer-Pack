# Authentication Prompts

## Build
Use the Authentication skill. Inspect existing project conventions, implement the requested change using idiomatic Authentication patterns, verify with token expiry tests, session invalidation checks, rate limit tests, MFA flows, and replay protection review, and summarize changed files and risks.

## Review
Use the Authentication skill. Review the change for correctness, maintainability, security, performance, and Authentication-specific pitfalls such as long-lived tokens, weak reset flows, missing lockout, insecure cookies, and confusing authentication with authorization. Provide findings by severity.

## Debug
Use the Authentication skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with token expiry tests, session invalidation checks, rate limit tests, MFA flows, and replay protection review.

## Document
Use the Authentication skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's Authentication implementation.


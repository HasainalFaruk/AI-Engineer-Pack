# Docker Prompts

## Build
Use the Docker skill. Inspect existing project conventions, implement the requested change using idiomatic Docker patterns, verify with docker build, compose smoke tests, image inspection, health checks, and dependency cache validation, and summarize changed files and risks.

## Review
Use the Docker skill. Review the change for correctness, maintainability, security, performance, and Docker-specific pitfalls such as running as root, copying secrets, bloated layers, missing health checks, and dev-only settings in production images. Provide findings by severity.

## Debug
Use the Docker skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with docker build, compose smoke tests, image inspection, health checks, and dependency cache validation.

## Document
Use the Docker skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's Docker implementation.


# React Prompts

## Build
Use the React skill. Inspect existing project conventions, implement the requested change using idiomatic React patterns, verify with component tests, accessibility checks, interaction tests, build checks, and visual smoke testing, and summarize changed files and risks.

## Review
Use the React skill. Review the change for correctness, maintainability, security, performance, and React-specific pitfalls such as state duplicated across components, unstable keys, missing labels, effect dependency bugs, and layout shifts. Provide findings by severity.

## Debug
Use the React skill. Reproduce or reason through the issue, inspect relevant files and runtime signals, identify the likely cause, apply a focused fix if requested, and verify with component tests, accessibility checks, interaction tests, build checks, and visual smoke testing.

## Document
Use the React skill. Create documentation that explains setup, usage, configuration, limitations, and troubleshooting for this repository's React implementation.

